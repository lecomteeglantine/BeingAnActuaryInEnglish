SESSION 6 — R5

Replace these four HTML files at the root of the GitHub repository.
Then open m1-day6.html?v=s6r5 (Ctrl+F5 if needed).

R5 audit fixes:
- Prevents an accidental double-click from advancing two steps in the final email builder.
- Makes the transition from email 5 to the final challenge explicit.
- Aligns the Helen Price 'over-formal' option with its 'workable but not ideal' feedback level.
- Cache version bumped to s6r5.

The game remains deterministic: no random scoring or device-dependent branch.
