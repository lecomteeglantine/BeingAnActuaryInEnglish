(function(){const all=window.ACTUARIAL_VOCAB||[];let deck=[...all].sort(()=>Math.random()-.5),i=0,back=false;const card=document.getElementById('flashcard'),prog=document.getElementById('progress');function render(){const v=deck[i];prog.textContent=`Card ${i+1} of ${deck.length}`;card.innerHTML=back?`<div><p class="tag">${v.cat}</p><h2>${v.fr}</h2><p>${v.def}</p><p><em>${v.ex}</em></p></div>`:`<div><p class="tag">${v.cat}</p><h2>${v.w}</h2><p class="ipa">${v.ipa}</p><p>Tap to reveal the meaning</p></div>`}function flip(){back=!back;render()}card.onclick=flip;card.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();flip()}};document.getElementById('flipBtn').onclick=flip;document.getElementById('nextBtn').onclick=()=>{i=(i+1)%deck.length;back=false;render()};document.getElementById('prevBtn').onclick=()=>{i=(i-1+deck.length)%deck.length;back=false;render()};document.getElementById('shuffleBtn').onclick=()=>{deck.sort(()=>Math.random()-.5);i=0;back=false;render()};render()})();
