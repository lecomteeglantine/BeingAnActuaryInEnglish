BeingAnActuaryInEnglish — QA / BUGFIX REPORT
Date: 2026-09-03

SCOPE
- Full static site audit after deployment of M1 Session 1.
- 24 HTML pages, shared CSS/JS, M1/M2 pages, dictionary, flashcards, games, pronunciation, notebook, PWA/service worker, and Session 1 individual/group activities.

CONFIRMED BUGS FIXED
1. AUDIO LATENCY
- Reworked speech synthesis to avoid re-querying/choosing a remote voice on every click.
- Prioritises an on-device British English voice, then another local English voice.
- Voice list is primed early and cached.
- Avoids unnecessary speechSynthesis.cancel() calls.
- Fast retry fallback if a selected voice errors.
- Sound-off now stops current speech cleanly.

2. M1 PAGE CONTRADICTION
- Removed copy saying no activity/theme existed while Session 1 was already live.
- Session 1 is now clearly marked as available; Sessions 2–5 remain intentionally unbuilt.

3. SHARED-COMPUTER SESSION STATE
- New launches of the individual/group activities from Session 1 start clean.
- A page reload/back-forward navigation preserves the current student's/group's work.
- Prevents the next class group inheriting the previous group's answers when starting normally from Session 1.

4. INDIVIDUAL ACTIVITY STATE / TIMER
- Elevator challenge completion restores correctly after refresh.
- Timer finish is guarded against duplicate completion.
- Progress bar width is clamped.
- Progress dots expose the current step accessibly.
- Restart now asks for confirmation and stops active speech/timer.
- Stage 1 wording no longer claims that listening to every word is technically enforced.

5. GROUP ACTIVITY STATE / BRIEF
- Selections and typed content persist during a reload.
- Generated final brief is restored after a reload.
- Editing an answer invalidates/hides a stale brief until regenerated.
- Validation messages are clearer and announced accessibly.
- User-entered text is escaped before rendering.
- Clear-all asks for confirmation when work exists.
- Audio practice buttons have explicit accessible labels.

6. PWA / UPDATE RELIABILITY
- Cache version bumped to a new audit-fix version.
- Service-worker install no longer fails entirely if one cached resource fails: CORE caching uses Promise.allSettled().
- skipWaiting()/clients.claim() retained so new deployments take effect more reliably.
- Added .nojekyll for GitHub Pages deployment hygiene.

7. DOCUMENTATION
- README dictionary count corrected to 85 terms.
- README now mentions M1 Session 1 individual and group activities.

FINAL AUTOMATED STATIC QA
- HTML pages: 24
- Local SVG illustrations: 7
- Vocabulary entries: 85
- Service-worker CORE resources: 47
- Missing local links/assets: 0 detected
- Duplicate HTML IDs: 0 detected
- Unlabelled form controls/buttons in audit: 0 detected
- Missing image alt attributes in audit: 0 detected
- JavaScript syntax errors: 0 detected
- CSS parse errors: 0 detected
- Manifest JSON: valid
- Vocabulary duplicate/missing required fields: 0 detected
- Service-worker CORE entries missing on disk: 0 detected
- Local SVG XML parsing errors: 0 detected

TESTING LIMITATION
Automated Chromium end-to-end rendering against localhost/file URLs was blocked by this execution environment's browser policy (ERR_BLOCKED_BY_ADMINISTRATOR). Static, syntax, state-logic and asset/link audits were completed instead. A final spot-check on the deployed GitHub Pages site after upload is still recommended for browser-specific speech voice availability, because speech synthesis voices depend on the student's OS/browser.
